const state = {
  allPosts: []
};

const getters = {
  getPosts: (state) => state.allPosts
};

const actions = {
};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
