const state = {
  allMessages: []
};

const getters = {
  getMessages: (state) => state.allMessages
};

const actions = {
};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
